<h1>All Groups</h1>

<% @groups.each do |group| %>
  <h2><%= link_to group.title, group %></h2>
  <p><%= group.description %></p>
  <%= link_to 'Dashboard', group_dashboard_path(group) %>

<% end %>

