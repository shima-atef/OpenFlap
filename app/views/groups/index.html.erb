<h1>All Groups</h1>

<% @groups.each do |group| %>
  <h2><%= link_to group.title, group %></h2>
  <p><%= group.description %></p>
  <h3><%= link_to group.title, group_dashboard_path(group_id: group.id) %></h3>

<% end %>

